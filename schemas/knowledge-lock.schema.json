{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://regen.engineering/schemas/0.1/knowledge-lock.schema.json",
  "title": "Regen Engineering knowledge.lock",
  "description": "Regeneration lineage for one module: what knowledge and which model produced the current implementation.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "module",
    "knowledge_version",
    "generated_by",
    "generated_at",
    "drift"
  ],
  "properties": {
    "module": {
      "type": "string",
      "minLength": 1,
      "description": "Module name. Must match the directory that owns this lock file."
    },
    "stack": {
      "type": "string",
      "minLength": 1,
      "description": "Implementation this lock describes, e.g. typescript. Required when a module has more than one implementation, in which case each lock is named knowledge.<stack>.lock."
    },
    "knowledge_version": {
      "type": "string",
      "pattern": "^[0-9a-f]{7,40}$",
      "description": "Git commit hash of the knowledge tree this implementation was generated from."
    },
    "generated_by": {
      "type": "string",
      "minLength": 1,
      "description": "Model or tool identifier, e.g. claude-fable-5."
    },
    "generated_at": {
      "type": "string",
      "format": "date",
      "pattern": "^[0-9]{4}-[0-9]{2}-[0-9]{2}$"
    },
    "contracts_passed": {
      "type": "array",
      "items": {
        "type": "string",
        "pattern": "^CT-[0-9]{3,}$"
      },
      "uniqueItems": true,
      "description": "Contract IDs green at generation time."
    },
    "drift": {
      "enum": [
        "none",
        "code-ahead",
        "knowledge-ahead"
      ],
      "description": "none: in sync. code-ahead: implementation changed without a knowledge delta, a defect. knowledge-ahead: knowledge changed, regeneration pending, a backlog."
    },
    "last_regeneration": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "at",
        "model",
        "result"
      ],
      "description": "Outcome of the most recent Regeneration Test for this module and stack. Absent means never attempted, which is not the same as passing.",
      "properties": {
        "at": {
          "type": "string",
          "pattern": "^[0-9]{4}-[0-9]{2}-[0-9]{2}$"
        },
        "model": {
          "type": "string",
          "minLength": 1
        },
        "result": {
          "enum": [
            "pass",
            "fail"
          ]
        },
        "contracts_passed": {
          "type": "integer",
          "minimum": 0
        },
        "contracts_total": {
          "type": "integer",
          "minimum": 0
        },
        "guesses": {
          "type": "integer",
          "minimum": 0,
          "description": "Questions the regenerating agent had to guess at. A pass with many guesses is a module that got lucky, so this is tracked separately from the result."
        }
      }
    },
    "drift_debt": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "since",
        "reason"
      ],
      "description": "Present only when drift is code-ahead and was accepted deliberately, e.g. an emergency hotfix awaiting reconciliation.",
      "properties": {
        "since": {
          "type": "string",
          "pattern": "^[0-9]{4}-[0-9]{2}-[0-9]{2}$"
        },
        "reason": {
          "type": "string",
          "minLength": 1
        },
        "reconciliation_task": {
          "type": "string"
        }
      }
    },
    "implementation_paths": {
      "type": "array",
      "items": {
        "type": "string",
        "minLength": 1
      },
      "uniqueItems": true,
      "description": "Repository-relative path prefixes holding this module's implementation, when it does not live under the module directory. Drift detection is blind to code outside a module unless it is declared here."
    }
  },
  "allOf": [
    {
      "if": {
        "properties": {
          "drift_debt": {
            "type": "object"
          }
        },
        "required": [
          "drift_debt"
        ]
      },
      "then": {
        "properties": {
          "drift": {
            "const": "code-ahead"
          }
        }
      }
    }
  ]
}
